apiVersion: apps/v1
kind: Deployment
metadata:
  name: bully-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: bully-app
  template:
    metadata:
      labels:
        app: bully-app
    spec:
      containers:
        - name: bully-container
          image: bully-app
          imagePullPolicy: Never
          env:
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: WEB_PORT
              value: "8080"
          ports:
            - containerPort: 8080
              name: http
